apiVersion: v1
kind: Service
metadata:
  name: {{ include "gaussos.fullname" . }}
  labels:
    {{- include "gaussos.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "gaussos.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
